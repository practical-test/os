1. echo - The 'echo' command helps us move some data, usually text into file
2. for - The 'for' command is used to repeatedly execute the set of commandsfor every element in the list
3. do - The 'do' command is used to execute any other commands on the system and capture the output
