echo "Menu based calculator"
echo "Enter the operands"
read a
read b
echo "Enter the operator"
read o
case $o in
"+") echo "$a + $b" = $(($a+$b));;
"-") echo "$a - $b" = $(($a-$b));;
"*") echo "$a * $b" = $(($a*$b));;
"/") echo "$a / $b" = $(($a/$b));;
*) echo "Invalid operation"
esac
